name: Jenkins Build

on:
  push:
    branches:
      - main

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST \
            "https://kuruma-jenkins.zigexn.vn/job/Kuruma%20Tools/job/test/build?token=12345678"
